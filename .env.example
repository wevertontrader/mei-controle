# Em desenvolvimento, deixe comentado para usar o proxy do Vite (padrão).
# Só defina se o backend rodar em outra porta (ex: 3002):
# VITE_API_URL=http://localhost:3002/api
# VITE_PROXY_TARGET=http://localhost:3002
